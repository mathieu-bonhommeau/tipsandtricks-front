version: "3"

services:
    webapp:
        build:
            context: .
            dockerfile: docker/webapp/Dockerfile

        container_name: "webapp_react"
        ports:
            - "3000:3000"

