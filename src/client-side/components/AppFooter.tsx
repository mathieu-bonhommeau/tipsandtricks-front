import {Container, Typography} from '@mui/material';

function AppFooter() {

    return (
        <Container >
            <Typography>Footer</Typography>
        </Container>
    );
}

export default AppFooter;
